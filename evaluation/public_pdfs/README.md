Place optional test PDFs here. **`generated_sample_report.pdf`** can be recreated with `python scripts/make_sample_pdf.py` (copied by `scripts/download_eval_pdfs.sh`).

Larger public samples (IRS form, academic paper) are **not** committed by default; run `bash scripts/download_eval_pdfs.sh` or download manually — see **`../README.md`**.
